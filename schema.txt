# schema.txt
#
# NOTE: This file previously contained misplaced content (AI image generation prompts
# and a third-party API key from an unrelated project). That content has been removed.
#
# The canonical Passagr database schema is maintained in:
#   supabase/migrations/  (Supabase SQL migrations — authoritative)
#   migrations/archive/   (legacy node-pg-migrate JS migrations)
#
# See also: module_manifest.md for a full file-by-file inventory of the codebase.
